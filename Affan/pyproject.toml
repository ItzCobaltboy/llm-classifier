[project]
name = "llm-classifer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
    "pyarrow>=23.0.1",
    "sentence-transformers>=2.7.0",
    "transformers>=4.51.0",
]
