# llm-classifier
A classifier to predict which LLM generate the text

## Dataset
For consistency using the directory `Dataset` which contains `Dataset_original`, `Dataset_medium` and `Dataset_small` folders. Which in turn contain`train`, `test` and `val` folders with corresponding .paraquet files. 
