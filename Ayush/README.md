# Change Column names of original dataset
Change the path in `change_columns_of_original_dataset.py` to wherever your data is stored, and then run the script to change the columns

# Data Transformation
Run `data_transformation.py` for data transformation. Make sure to change max_length according to your need.
